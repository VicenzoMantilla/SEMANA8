/*
Create two String variables and add the length
 of each variable (number of letters in the string) and store the result in a 3rd variable (use length).
 */

 var text1='Telescopio';
 var text2='Celular';

 var result= text1 + text2;

 console.log(' The lengt of "result" is: ' + result.length);