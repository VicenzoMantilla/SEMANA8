/* Strings:
Create a string variable with at least 10 characters
and generate a new string with the last 3 characters saving the result in a new variable (use substring).
*/

var text1='Electrotecnia';

var textf= text1.substring(10,13);

console.log('The new word is: ' + textf);