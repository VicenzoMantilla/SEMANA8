/* Strings:
Create a string variable with at least 10 characters and generate
a new string with the first 5 characters saving the result in a new variable (use substring).
*/

var text1='enciclopedia';

var text2= text1.substring(0,5);

console.log('The new word is: ' + text2);